import ContractsManager from "@/components/cadastros/ContractsManager";

/**
 * @description Um componente de página que renderiza o ContractsManager para gerenciar contratos de fornecedores.
 * @returns O componente ContractsManager.
 */
export default function FornecedoresContratos() {
  return <ContractsManager />;
}
